<Window x:Class="QuickLookNext.ViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:busyDecorator="clr-namespace:QuickLookNext.Controls.BusyDecorator"
        xmlns:controls="clr-namespace:QuickLookNext.Controls"
        xmlns:converters="clr-namespace:QuickLookNext.Converters"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:glassLayer="clr-namespace:QuickLookNext.Controls.GlassLayer"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
        x:Name="mainWindow"
        Title="QuickLook-Next"
        MinWidth="400"
        MinHeight="200"
        d:DesignHeight="700"
        d:DesignWidth="624"
        Background="{DynamicResource MainWindowBackground}"
        BorderBrush="{DynamicResource CaptionBackground}"
        Foreground="{DynamicResource WindowTextForeground}"
        ResizeMode="CanResize"
        ShowActivated="False"
        ShowInTaskbar="False"
        UseLayoutRounding="True"
        mc:Ignorable="d">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/QuickLook.Common;component/Styles/MainWindowStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <converters:WindowStateMaximizedToThicknessConverter x:Key="WindowStateMaximizedToThicknessConverter" />
            <converters:WindowStateNormalToThicknessConverter x:Key="WindowStateNormalToThicknessConverter" />
            <converters:BooleanToVisibilityCollapsedConverter x:Key="BooleanToVisibilityCollapsedConverter" />
            <converters:BooleanToVisibilityHiddenConverter x:Key="BooleanToVisibilityHiddenConverter" />
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <!-- v3.9.0: subtle fade-in of the preview content once it becomes
                 ready (tied to the ShowWindowTransition option). -->
            <Storyboard x:Key="ContentFadeInStoryboard">
                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                 From="0.85"
                                 To="1"
                                 Duration="0:0:0.12" />
            </Storyboard>
        </ResourceDictionary>
    </Window.Resources>
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="{StaticResource MainWindowCaptionHeight}"
                      CornerRadius="0"
                      GlassFrameThickness="1"
                      ResizeBorderThickness="{StaticResource MainWindowResizeThickness}"
                      UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>
    <Window.Style>
        <Style TargetType="Window">
            <Setter Property="BorderThickness" Value="{StaticResource MainWindowShadowMarginThickness}" />
        </Style>
    </Window.Style>
    <Grid x:Name="windowFrameContainer" Margin="{Binding ElementName=mainWindow, Path=WindowState, Converter={StaticResource WindowStateMaximizedToThicknessConverter}, ConverterParameter={StaticResource MainWindowResizeThickness}}">
        <Grid x:Name="viewerRootContainer" ZIndex="190">
            <Grid x:Name="windowCaptionContainer"
                  Height="{StaticResource MainWindowCaptionHeight}"
                  VerticalAlignment="Top"
                  ZIndex="100">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Opacity" Value="1" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ContextObject.TitlebarAutoHide, ElementName=mainWindow}" Value="True">
                                <Setter Property="Opacity" Value="0" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <Grid.Resources>
                    <!-- v5.4.0: both transitions are plain 50 ms fades. Hiding is no longer part of
                         this storyboard (its Completed handler used to start the hide), because the
                         poll re-showed the bar the moment the hide began - with the cursor parked on
                         the bar that looped forever. Who hides it now: the poll, once the cursor has
                         left the top zone (see ViewerWindow.StartTopBarPolling). -->
                    <Storyboard x:Key="ShowCaptionContainerStoryboard">
                        <DoubleAnimation Storyboard.Target="{Binding Source={x:Reference windowCaptionContainer}}"
                                         Storyboard.TargetProperty="Opacity"
                                         To="1"
                                         Duration="0:0:0.05" />
                    </Storyboard>
                    <Storyboard x:Key="HideCaptionContainerStoryboard">
                        <DoubleAnimation Storyboard.Target="{Binding Source={x:Reference windowCaptionContainer}}"
                                         Storyboard.TargetProperty="Opacity"
                                         To="0"
                                         Duration="0:0:0.05" />
                    </Storyboard>
                </Grid.Resources>
                <!-- v5.3.0: the scrim is the bar's own floor. Plugins that switch the glass off
                     (the image viewer) used to leave the icons floating on the picture itself. -->
                <Border Background="{DynamicResource CaptionScrimBrush}" />
                <glassLayer:GlassLayer BlurredElement="{Binding ElementName=containerPanel}"
                                       ColorOverlayVisibility="{Binding ContextObject.TitlebarColourVisibility, ElementName=mainWindow, Converter={StaticResource BooleanToVisibilityConverter}}"
                                       GlassVisibility="{Binding ContextObject.TitlebarBlurVisibility, ElementName=mainWindow, Converter={StaticResource BooleanToVisibilityConverter}}"
                                       NoiseVisibility="{Binding ContextObject.TitlebarBlurVisibility, ElementName=mainWindow, Converter={StaticResource BooleanToVisibilityConverter}}"
                                       OverlayColor="{DynamicResource CaptionBackground}" />
                <DockPanel>
                    <Button x:Name="buttonCloseWindow"
                            Content="&#xE894;"
                            DockPanel.Dock="Right"
                            Style="{StaticResource CaptionCloseButtonStyle}" />
                    <Button x:Name="buttonWindowStatus"
                            DockPanel.Dock="Right"
                            Visibility="{Binding ContextObject.CanResize, ElementName=mainWindow, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Button.Style>
                            <Style BasedOn="{StaticResource CaptionButtonStyle}" TargetType="Button">
                                <Setter Property="Content" Value="&#xE740;" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding WindowState, ElementName=mainWindow}" Value="Maximized">
                                        <Setter Property="Content" Value="&#xE73F;" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button x:Name="buttonTheme"
                            Content="&#xE706;"
                            DockPanel.Dock="Right"
                            Style="{StaticResource CaptionButtonStyle}"
                            ToolTip="Toggle Light/Dark Theme" />
                    <!-- v5.3.0: the bar now reads as two groups - what you can do with the file
                         (share / open / open with / reload / OCR / more) and the window itself
                         (theme / maximize / close) - separated by this hairline. -->
                    <Border DockPanel.Dock="Right"
                            Width="1"
                            Height="16"
                            Margin="6,0,6,0"
                            VerticalAlignment="Center"
                            Background="{DynamicResource CaptionSeparatorBrush}" />
                    <Button x:Name="buttonShare"
                            Content="&#xE72D;"
                            DockPanel.Dock="Right"
                            Style="{StaticResource CaptionButtonStyle}"
                            ToolTip="Share" />
                    <Button x:Name="buttonOpenWith"
                            Content="&#xE7AC;"
                            DockPanel.Dock="Right"
                            Style="{StaticResource CaptionButtonStyle}"
                            ToolTip="Open With" />
                    <Button x:Name="buttonOpen"
                            Content="&#xE8E5;"
                            DockPanel.Dock="Right"
                            Style="{StaticResource CaptionButtonStyle}"
                            ToolTip="Open with XXX" />
                    <Button x:Name="buttonReload"
                            Content="&#xE72C;"
                            DockPanel.Dock="Right"
                            Style="{StaticResource CaptionButtonStyle}"
                            ToolTip="Reload" />
                    <!-- v5.0.11: text recognition for image previews. It used to live in the
                         More menu, where nobody found it; the visibility is set per preview
                         (only the image viewer can produce it). -->
                    <Button x:Name="buttonOcr"
                            Content="&#xE8FE;"
                            DockPanel.Dock="Right"
                            Style="{StaticResource CaptionButtonStyle}"
                            Visibility="Collapsed"
                            ToolTip="Extract text" />
                    <Button x:Name="buttonMore"
                            Content="&#xE712;"
                            DockPanel.Dock="Right"
                            Style="{StaticResource CaptionButtonStyle}"
                            ToolTip="More" />
                    <Button x:Name="buttonTop"
                            DockPanel.Dock="Left"
                            Tag="Auto"
                            ToolTip="Stay on Top">
                        <!-- v5.3.0: one glyph per button, state shown by the accent colour. The
                             old "big glyph + shrunken glyph badge" pair (a 0.25-scaled F83F /
                             E842 drawn on top) read as noise at 100% scaling, and the two left
                             buttons were the only place in the app using that trick. -->
                        <Button.Style>
                            <Style BasedOn="{StaticResource CaptionButtonStyle}" TargetType="Button">
                                <Setter Property="Content" Value="&#xE74A;" />
                                <Style.Triggers>
                                    <Trigger Property="Tag" Value="Auto">
                                        <Setter Property="Content" Value="&#xE74A;" />
                                    </Trigger>
                                    <Trigger Property="Tag" Value="Top">
                                        <Setter Property="Content" Value="&#xE74A;" />
                                        <Setter Property="Foreground"
                                                Value="{DynamicResource CaptionAccentBrush}" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button x:Name="buttonPin"
                            DockPanel.Dock="Left"
                            Tag="Auto"
                            ToolTip="Prevent Closing">
                        <Button.Style>
                            <Style BasedOn="{StaticResource CaptionButtonStyle}" TargetType="Button">
                                <Setter Property="Content" Value="&#xE718;" />
                                <Style.Triggers>
                                    <Trigger Property="Tag" Value="Auto">
                                        <Setter Property="Content" Value="&#xE718;" />
                                    </Trigger>
                                    <Trigger Property="Tag" Value="Pin">
                                        <Setter Property="Content" Value="&#xE840;" />
                                        <Setter Property="Foreground"
                                                Value="{DynamicResource CaptionAccentBrush}" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Grid x:Name="titleArea" Background="Transparent">
                        <TextBlock x:Name="titleAreaText"
                                   Margin="5,0"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Center"
                                   FontSize="12"
                                   TextTrimming="CharacterEllipsis" />
                    </Grid>
                </DockPanel>
            </Grid>
            <DockPanel x:Name="containerPanel" ZIndex="80">
                <Grid Height="{StaticResource MainWindowCaptionHeight}"
                      DockPanel.Dock="Top"
                      Visibility="{Binding ContextObject.TitlebarOverlap, ElementName=mainWindow, Converter={StaticResource BooleanToVisibilityCollapsedConverter}}" />
                <ContentControl x:Name="container"
                                Content="{Binding ContextObject.ViewerContent, ElementName=mainWindow}"
                                Foreground="{DynamicResource WindowTextForeground}" />
            </DockPanel>
        </Grid>
        <Grid x:Name="busyIndicatorLayer"
              Visibility="Collapsed"
              ZIndex="200">
            <!-- v1.2.14: in-process spinner. The previous BusyDecorator rendered
                 through a separate-thread HostVisual; combined with the old
                 "hide content while busy" binding this produced the gray
                 loading area during preview switches. -->
            <busyDecorator:SpinIcon x:Name="busySpinner"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    IsHitTestVisible="False"
                                    FontFamily="{StaticResource SegoeFluent}"
                                    FontSize="30"
                                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                    Opacity="0.9"
                                    Spin="{Binding ElementName=mainWindow, Path=ContextObject.IsBusy}"
                                    SpinDuration="1.2"
                                    Text="&#xE72C;">
                <busyDecorator:SpinIcon.Effect>
                    <DropShadowEffect BlurRadius="8"
                                      ShadowDepth="1"
                                      Opacity="0.45"
                                      Color="Black" />
                </busyDecorator:SpinIcon.Effect>
            </busyDecorator:SpinIcon>
        </Grid>
    </Grid>
</Window>
